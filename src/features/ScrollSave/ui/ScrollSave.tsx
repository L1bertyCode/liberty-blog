import { memo } from "react";
import { useTranslation } from "react-i18next";

export const ScrollSave = memo(() => {
  const { t } = useTranslation();
  return (
    <div>
      <div>ScrollSave</div>
    </div>
  );
});
